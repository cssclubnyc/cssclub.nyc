<style>
  .container {
    align-items: flex-start;
    background-color: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(1px);
    display: flex;
    height: 100%;
    justify-content: center;
    position: absolute;
    width: 100%;
    z-index: 100;
  }

  .contents {
    background-color: var(--color-white);
    border-radius: var(--radius-low);
    border: 1px solid var(--color-accent-info-low);
    background-color: var(--color-accent-info);
    border-radius: var(--radius-low);
    font-size: var(--font-size-16);
    padding: var(--sp2);
    margin-top: 20%;
    max-width: 93%;
  }
</style>

<div class="container">
  <div class="contents">
    <p>
      We did a couple of these and they were fun, but needed to pause. If you’re interested in starting something like this back up, or just have questions about how this all went, email Tyler at me@tylergaw.com.
    </p>
  </div>
</div>